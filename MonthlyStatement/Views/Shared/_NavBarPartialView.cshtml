@{
    string avt = "";
    avt = "~/assets/img/user.png";
}

<nav class="navbar navbar-expand-lg main-navbar sticky">
    <div class="form-inline mr-auto">
        <ul class="navbar-nav mr-3">
            <li>
                <a href="#" data-toggle="sidebar" class="nav-link nav-link-lg
									collapse-btn"> <i data-feather="align-justify"></i></a>
            </li>
            <li>
                <a href="#" class="nav-link nav-link-lg fullscreen-btn">
                    <i data-feather="maximize"></i>
                </a>
            </li>
        </ul>
    </div>
    <ul class="navbar-nav navbar-right">
        <li class="dropdown dropdown-list-toggle">
            <a href="#" data-toggle="dropdown"
               class="nav-link notification-toggle nav-link-lg">
                <i data-feather="bell" class="bell"></i>
            </a>
            <div class="dropdown-menu dropdown-list dropdown-menu-right pullDown">
                <div class="dropdown-header">
                    Thông báo
                </div>
                <div class="dropdown-footer text-center">
                    <a href="">Xem tất cả<i class="fas fa-chevron-right"></i></a>
                </div>
            </div>
        </li>
        <li class="dropdown">
            <a href="#" data-toggle="dropdown"
               class="nav-link dropdown-toggle nav-link-lg nav-link-user">
                <img alt="image" src="@Url.Content(avt)" class="rounded-circle img-fluid"> <span class="d-sm-none d-lg-inline-block"></span>
            </a>
            <div class="dropdown-menu dropdown-menu-right pullDown">
                <a href="@Url.Action("Index", "Profile",new { Area = "" })" class="dropdown-item has-icon">
                    <i class="far fa-user"> </i>
                    Hồ sơ
                </a>
                @*<a href="timeline.html" class="dropdown-item has-icon">
                    <i class="fas fa-bolt"></i>
                    activities
                </a>
                <a href="#" class="dropdown-item has-icon">
                    <i class="fas fa-cog"></i>
                    settings
                </a>*@
                <div class="dropdown-divider"></div>
                @using (@Html.BeginForm("SignOut", "Account", new { Area = "" }, FormMethod.Post, new { id = "logoutForm" }))
                {
                    <a href="javascript:document.getElementById('logoutForm').submit()" class="dropdown-item has-icon text-danger"><i class="ft-power mr-2"></i><span>Đăng xuất</span></a>
                }
            </div>

        </li>
        <li>
            <div>
                @User.Identity.Name
            </div>
            <div>
                Vai trò:
                @(User.IsInRole("Quản trị viên") ? "Quản trị viên" :
                User.IsInRole("Ban phòng khoa" )? "Ban phòng khoa" :
                User.IsInRole("Thư ký") ? "Thư ký" :
                User.IsInRole("Bộ môn") ? "Bộ môn" :
                User.IsInRole("Nhân viên - Giảng viên") ? "Nhân viên - Giảng viên" : "Chưa cấp quyền")
            </div>
        </li>
    </ul>
</nav>
